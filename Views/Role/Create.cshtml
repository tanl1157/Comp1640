@model Microsoft.AspNetCore.Identity.IdentityRole

@{
    ViewData["Title"] = "Create Role";
}



<div class="container py-5">
    <div class="text-center mb-4">
        <h1 class="text-primary">Create Role</h1>
        <p class="text-muted">Fill out the form below to create a new role for the system.</p>
    </div>

    <form asp-action="Create" class="row justify-content-center">
        <!-- Validation Summary -->
        <div asp-validation-summary="ModelOnly" class="alert alert-danger col-md-8" role="alert"></div>

        <!-- Form Fields -->
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-body">
                    <div class="form-group mb-4">
                        <label asp-for="Id" class="form-label">Role ID</label>
                        <input asp-for="Id" class="form-control" />
                        <span asp-validation-for="Id" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-4">
                        <label asp-for="Name" class="form-label">Role Name</label>
                        <input asp-for="Name" class="form-control" placeholder="Enter role name" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a asp-action="Index" class="btn btn-secondary">
                            <span class="material-icons">arrow_back</span> Back
                        </a>
                        <button type="submit" class="btn btn-success">
                            <span class="material-icons">add</span> Create
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
