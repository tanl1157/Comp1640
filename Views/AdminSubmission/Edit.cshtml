@model Idea.Models.Submission

@{
    ViewData["Title"] = "Edit Submission";
}

<div class="container py-5">
    <div class="text-center mb-4">
        <h1 class="text-primary">Edit Submission</h1>
        <p class="text-muted">Update the information for the selected submission and save your changes.</p>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-body">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                        <input type="hidden" asp-for="Id" />

                        <div class="form-group mb-3">
                            <label asp-for="Name" class="form-label fw-bold">Submission Name</label>
                            <input asp-for="Name" class="form-control" placeholder="Enter submission name" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Deadline_1" class="form-label fw-bold">First Deadline</label>
                            <input asp-for="Deadline_1" class="form-control" type="datetime-local" />
                            <span asp-validation-for="Deadline_1" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Deadline_2" class="form-label fw-bold">Second Deadline</label>
                            <input asp-for="Deadline_2" class="form-control" type="datetime-local" />
                            <span asp-validation-for="Deadline_2" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-secondary">
                                <span class="material-icons">arrow_back</span> Back to List
                            </a>
                            <button type="submit" class="btn btn-success">
                                <span class="material-icons">save</span> Save Changes
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}